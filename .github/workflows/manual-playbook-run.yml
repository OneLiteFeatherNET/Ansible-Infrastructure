name: Manual Playbook Run

on:
  workflow_dispatch:

jobs:
  run-bedrock:
    name: Run Bedrock Playbook
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Ansible
        uses: dawidd6/action-ansible-playbook@v2
        with:
          version: '2.11'

      - name: Install Ansible Galaxy Roles
        run: ansible-galaxy install -r requirements.yml

      - name: Decrypt Ansible Vault
        run: echo $ANSIBLE_VAULT_PASSWORD > .vault-password
        env:
          ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}

      - name: Run Bedrock Playbook
        run: ansible-playbook -i inventory-vault.yml bedrock.yml --check

  # Add additional playbooks as separate jobs
  # Example:
  # run-another-playbook:
  #   name: Run Another Playbook
  #   runs-on: self-hosted
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v2
  #
  #     - name: Set up Ansible
  #       uses: dawidd6/action-ansible-playbook@v2
  #       with:
  #         version: '2.11'
  #
  #     - name: Install Ansible Galaxy Roles
  #       run: ansible-galaxy install -r requirements.yml
  #
  #     - name: Decrypt Ansible Vault
  #       run: echo $ANSIBLE_VAULT_PASSWORD > .vault-password
  #       env:
  #         ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
  #
  #     - name: Run Another Playbook
  #       run: ansible-playbook -i inventory-vault.yml another-playbook.yml --ask-vault-pass
  #       env:
  #         ANSIBLE_VAULT_PASSWORD: ${{ secrets.ANSIBLE_VAULT_PASSWORD }}
